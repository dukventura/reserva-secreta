# Copie para .env.local (nunca commitado) para apontar o front-end
# para a API. Em producao, a mesma variavel e' definida no ambiente do
# build (ex: secret/variable do GitHub Actions).
VITE_API_URL=http://localhost:3001
